import { LiveEquipmentRegistrationPanel } from '../../LiveEquipmentRegistrationPanel'

/** Desktop registration mount. The shared registration business flow stays common; desktop owns where it is composed and styled. */
export function EquipmentDesktopRegistration() {
  return <LiveEquipmentRegistrationPanel />
}
