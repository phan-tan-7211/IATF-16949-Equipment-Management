import { LiveEquipmentRegistrationPanel } from '../../LiveEquipmentRegistrationPanel'

/** Mobile registration mount. The shared registration business flow stays common; mobile owns where it is composed and styled. */
export function EquipmentMobileRegistration() {
  return <LiveEquipmentRegistrationPanel />
}
